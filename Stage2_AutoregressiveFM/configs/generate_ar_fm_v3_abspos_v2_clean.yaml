# Auto-generated from generate_ar_fm_v3_abspos_healthy.yaml by make_v2_clean_generate_configs.py
# v2_clean dataset variant (biffilter_v1).
# Checkpoints come from the corresponding *_v2_clean training runs.
# feature_stats / aneu_label_dir intentionally left at the original
# healthy paths — the v2_clean training used them as-is.
data:
  absolute_positions: true
paths:
  checkpoint: Stage2_AutoregressiveFM/output/ar_fm_v3_abspos_v2_clean/best_model.pt
  val_dir: derived_data/TreesSplines_k_count_100depth_prepared_biffilter_v1_norm_relpos_nodecp_v1/test
  feature_stats: Stage2_FlowMatching/feature_stats_healthy_abspos.npz
model:
  geom_dim: 39
  k_classes: 3
  max_depth: 128
  d_model: 256
  n_heads: 8
  n_layers: 6
  d_ff: 1024
  max_nodes: 256
  dropout: 0.25
  self_conditioning: true
params:
  n_samples: 100
  n_steps: 50
  seed: 42
  device: 0
  use_ema: true
  rp_damp: 0.75
  velocity_scale: 1.0
  output_dir: Stage2_AutoregressiveFM/generated/ar_fm_v3_abspos_v2_clean
