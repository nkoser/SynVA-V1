# Trim TreesSplines to depth 100 for prepared_meshes_3 run.

paths:
  input_dir: '/data/Output_minimal_data_prepared_meshs3/TreesSplines'
  output_dir: '/data/Output_minimal_data_prepared_meshs3/TreesSplines_k_count_100depth'
 
params:
  max_depth: 100
  k: 39
  input_mode: post_order
  output_mode: pre_order_kcount
  glob: '*.npy'
  overwrite: false
  log_every: 1
