# Feature normalization, v4-equivalent (geometry_clip, knots untouched).
# Output dataset name uses "v4" instead of "v4".
paths:
  train_dir: /data/Output_minimal_data_prepared_meshs3/TreesSplines_k_count_100depth_prepared/train
  val_dir:   /data/Output_minimal_data_prepared_meshs3/TreesSplines_k_count_100depth_prepared/val
  test_dir:  /data/Output_minimal_data_prepared_meshs3/TreesSplines_k_count_100depth_prepared/test

  output_root: /data/Output_minimal_data_prepared_meshs3/TreesSplines_k_count_100depth_prepared_norm_v4
  stats_path:  /data/Output_minimal_data_prepared_meshs3/TreesSplines_k_count_100depth_prepared_norm_v4/normalization_stats.json

params:
  pattern: "*.npy"

  method: geometry_clip
  quantile: 99.9
  clip_value: 3.0

  feature_start: 1
  feature_end: null

  geometry_isotropic: true
  geometry_normalize_knots: false
  geometry_knots_quantile: 99.9

  preserve_root_zero_xyz: true
  root_position: first
  root_xyz_indices: [1, 2, 3]
  root_eps: 1e-8

  eps: 1e-6
  in_place: false
  overwrite: false
